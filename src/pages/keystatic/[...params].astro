---
import { makePage } from '@keystatic/astro/ui';
import keystaticConfig from '../../../keystatic.config';

export const all = makePage(keystaticConfig);
export const getStaticPaths = all.getStaticPaths;
---
