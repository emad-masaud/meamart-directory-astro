---
import Layout from "~/layouts/Layout.astro";
import Container from "~/components/ui/Container.astro";
import PageHeader from "~/components/layout/PageHeader.astro";
import Button from "~/components/ui/Button.astro";
import { getMeamartSession } from "~/utils/session";
import { defaultLang } from "~/i18n/ui";
import { isRtl, useTranslations } from "~/i18n/utils";
import {
  ArrowLeft,
  ArrowRight,
  Save,
  Image as ImageIcon,
  Barcode,
  HelpCircle,
  Layers,
} from "lucide-react";
import IntentWorkflow from "~/components/react/IntentWorkflow";
import { AiWriter } from "~/components/catalog/AiWriter";

export const prerender = false;

const { lang = defaultLang } = Astro.params;
const rtl = isRtl(lang);
const t = useTranslations(lang);

let session = Astro.locals.userSession || (await getMeamartSession(Astro));

if (!session) {
  return Astro.redirect(`/${lang}/login`);
}

let categories: any[] = [];
try {
  categories = [];
} catch (e) {
  console.error("Failed to fetch categories:", e);
}

const aiTranslations = {
  'catalog.ai.title': (t as any)['catalog.ai.title'] || 'مساعد الذكاء الاصطناعي',
  'catalog.ai.desc': (t as any)['catalog.ai.desc'] || 'استخدم الذكاء الاصطناعي لكتابة وصف احترافي لمنتجك.',
  'catalog.ai.product_name': (t as any)['catalog.ai.product_name'] || 'اسم المنتج المبدئي',
  'catalog.ai.product_placeholder': (t as any)['catalog.ai.product_placeholder'] || 'مثال: حذاء نايك...',
  'catalog.ai.details': (t as any)['catalog.ai.details'] || 'تفاصيل إضافية (اختياري)',
  'catalog.ai.details_placeholder': (t as any)['catalog.ai.details_placeholder'] || 'اللون، المقاس، الخ...',
  'catalog.ai.generate': (t as any)['catalog.ai.generate'] || 'توليد المحتوى',
  'catalog.ai.generating': (t as any)['catalog.ai.generating'] || 'جاري التوليد...',
  'catalog.ai.output': (t as any)['catalog.ai.output'] || 'النتيجة',
  'catalog.ai.output_desc': (t as any)['catalog.ai.output_desc'] || 'انسخ الوصف والصقه في حقول الوصف أدناه.',
  'catalog.ai.empty_output': (t as any)['catalog.ai.empty_output'] || 'املأ البيانات واضغط توليد...',
};
---

<Layout title={t["catalog.create.1"]} description={t["catalog.create.2"]}>
  <PageHeader
    title={t["catalog.create.3"]}
    description={t["catalog.create.4"]}
  />

  <Container class="pb-24 max-w-3xl">
    <!-- Navigation Back -->
    <div class="mb-6">
      <a
        href={`/${lang}/seller/products`}
        class="inline-flex items-center gap-2 rounded-full border border-zinc-200 bg-white/80 py-2.5 px-5 text-xs font-bold text-zinc-700 hover:bg-zinc-50 transition-all dark:border-zinc-800 dark:bg-zinc-950/80 dark:text-zinc-300 dark:hover:bg-zinc-800"
      >
        {rtl ? <ArrowRight class="h-4 w-4" /> : <ArrowLeft class="h-4 w-4" />}
        <span>{t["catalog.create.5"]}</span>
      </a>
    </div>

    <div
      id="form-status"
      class="hidden p-4 mb-6 rounded-full text-sm font-semibold"
    >
    </div>

    <!-- Smart AI Assistant Widget -->
    <div class="mb-8">
      <IntentWorkflow client:idle lang={lang} />
    </div>

    <form id="create-product-form" class="space-y-8">
      <!-- Group 1: الأساسيات (Basic Details) -->
      <div
        class="rounded-3xl border border-zinc-200/50 bg-white/60 p-6 sm:p-8 shadow-lg backdrop-blur-md dark:border-zinc-800/50 dark:bg-zinc-900/60 space-y-6"
      >
        <h2
          class="text-lg font-bold text-zinc-900 dark:text-zinc-50 border-b border-zinc-100 pb-3 dark:border-zinc-800"
        >
          {t["catalog.create.6"]}
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <!-- Product Type Selection -->
          <div class="md:col-span-2">
            <label class="form-label">
              {t["catalog.create.7"]}
            </label>
            <select
              name="type"
              id="product-type-select"
              required
              class="form-select"
            >
              <option value="simple" selected>{t["catalog.create.8"]}</option>
              <option value="variable">{t["catalog.create.9"]}</option>
              <option value="virtual">{t["catalog.create.10"]}</option>
            </select>
          </div>

          <!-- Name -->
          <div class="md:col-span-2">
            <label class="form-label">
              {t["catalog.create.11"]}
            </label>
            <input
              type="text"
              name="name"
              required
              placeholder={t["catalog.create.12"]}
              class="form-input"
            />
          </div>

          <!-- SKU -->
          <div>
            <label class="form-label">
              {t["catalog.create.13"]}
            </label>
            <input
              type="text"
              name="sku"
              required
              placeholder="IPHONE15PM-256"
              class="form-input"
            />
          </div>

          <!-- Category -->
          <div>
            <label class="form-label">
              {t["catalog.create.14"]}
            </label>
            <select name="category" required class="form-select">
              <option value=""
                >{
                  t["ads.create.field.category.placeholder"] ||
                    "اختر التصنيف..."
                }</option
              >
              {categories.map((c) => <option value={c.id}>{c.name}</option>)}
            </select>
          </div>

          <!-- Regular Price -->
          <div id="regular-price-container">
            <label class="form-label">
              {t["catalog.create.16"]}
            </label>
            <input
              type="number"
              name="regular_price"
              required
              min="0"
              step="0.01"
              placeholder="4300"
              class="form-input"
            />
          </div>

          <!-- Sale Price -->
          <div id="sale-price-container">
            <label class="form-label">
              {t["catalog.create.17"]}
            </label>
            <input
              type="number"
              name="sale_price"
              min="0"
              step="0.01"
              placeholder="3999"
              class="form-input"
            />
          </div>

          <div class="md:col-span-2 my-4">
             <AiWriter client:load t={aiTranslations} />
          </div>

          <!-- Short Description -->
          <div class="md:col-span-2">
            <label class="form-label">
              {t["catalog.create.18"]}
            </label>
            <input
              type="text"
              name="short_description"
              required
              placeholder={t["catalog.create.19"]}
              class="form-input"
            />
          </div>

          <!-- Detailed Description -->
          <div class="md:col-span-2">
            <label class="form-label">
              {t["catalog.create.20"]}
            </label>
            <textarea
              name="description"
              rows="5"
              placeholder={t["catalog.create.21"]}
              class="form-textarea"></textarea>
          </div>
        </div>
      </div>

      <!-- Group 2: سكيما البيانات المعتمدة وقوقل (GS1 & Google Merchant Schema) -->
      <div
        class="rounded-3xl border border-zinc-200/50 bg-white/60 p-6 sm:p-8 shadow-lg backdrop-blur-md dark:border-zinc-800/50 dark:bg-zinc-900/60 space-y-6"
      >
        <h2
          class="text-lg font-bold text-zinc-900 dark:text-zinc-50 border-b border-zinc-100 pb-3 dark:border-zinc-800 flex items-center gap-2"
        >
          <Barcode class="h-5 w-5 text-primary animate-pulse" />
          <span>{t["catalog.create.22"]}</span>
        </h2>

        <div
          class="rounded-full bg-amber-50 p-4 border border-amber-200/60 dark:bg-amber-950/20 dark:border-amber-900/30 text-xs text-amber-800 dark:text-amber-300 space-y-1"
        >
          <p class="font-bold">{t["catalog.create.23"]}</p>
          <p>
            {t["catalog.create.24"]}
            <a
              href={`/${lang}/ads/create?categoryKey=buy-sell-misc`}
              class="font-extrabold text-primary underline ms-1 hover:text-primary/80 transition-colors"
            >
              {t["catalog.create.25"]}
            </a>
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <!-- GTIN / EAN / Barcode -->
          <div>
            <label
              class="flex items-center gap-1.5 text-xs font-bold text-zinc-600 dark:text-zinc-400 mb-2"
            >
              <span>{t["catalog.create.26"]}</span>
              <span
                class="cursor-help text-zinc-400"
                title={t["catalog.create.27"]}
                ><HelpCircle class="h-3.5 w-3.5" /></span
              >
            </label>
            <input
              type="text"
              name="gtin"
              required
              placeholder="6281000000000"
              pattern="^[0-9]{8,14}$"
              class="form-input"
            />
          </div>

          <!-- Brand & Image Upload (Mandatory) -->
          <div class="space-y-4">
            <div>
              <label class="form-label">
                {t["catalog.create.28"]}
              </label>
              <input
                type="text"
                name="brand"
                required
                placeholder={t["catalog.create.29"]}
                class="form-input"
              />
            </div>
            <div>
              <label class="form-label">
                {t["catalog.create.30"]}
              </label>
              <div
                class="relative w-full rounded-full border-2 border-dashed border-zinc-300 bg-white/40 p-4 text-center dark:border-zinc-700 dark:bg-zinc-900/40 hover:border-primary transition-all"
              >
                <input
                  type="file"
                  id="brand-image-input"
                  accept="image/*"
                  required
                  class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10"
                />
                <div
                  id="brand-image-placeholder"
                  class="text-xs text-zinc-450 dark:text-zinc-500 flex flex-col items-center gap-1"
                >
                  <span class="font-bold text-zinc-700 dark:text-zinc-300"
                    >{t["catalog.create.31"]}</span
                  >
                </div>
                <img
                  id="brand-image-preview"
                  class="hidden mx-auto h-16 w-16 object-contain rounded-xl border border-zinc-200 dark:border-zinc-800"
                />
              </div>
            </div>
          </div>

          <!-- Supplier & Image Upload (Mandatory) -->
          <div class="space-y-4">
            <div>
              <label class="form-label">
                {t["catalog.create.32"]}
              </label>
              <input
                type="text"
                name="supplier"
                required
                placeholder={t["catalog.create.33"]}
                class="form-input"
              />
            </div>
            <div>
              <label class="form-label">
                {t["catalog.create.34"]}
              </label>
              <div
                class="relative w-full rounded-full border-2 border-dashed border-zinc-300 bg-white/40 p-4 text-center dark:border-zinc-700 dark:bg-zinc-900/40 hover:border-primary transition-all"
              >
                <input
                  type="file"
                  id="supplier-image-input"
                  accept="image/*"
                  required
                  class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10"
                />
                <div
                  id="supplier-image-placeholder"
                  class="text-xs text-zinc-450 dark:text-zinc-500 flex flex-col items-center gap-1"
                >
                  <span class="font-bold text-zinc-700 dark:text-zinc-300"
                    >{t["catalog.create.35"]}</span
                  >
                </div>
                <img
                  id="supplier-image-preview"
                  class="hidden mx-auto h-16 w-16 object-contain rounded-xl border border-zinc-200 dark:border-zinc-800"
                />
              </div>
            </div>
          </div>

          <!-- MPN -->
          <div>
            <label class="form-label">
              {t["catalog.create.36"]}
            </label>
            <input
              type="text"
              name="mpn"
              placeholder="MU663ZA/A"
              class="form-input"
            />
          </div>

          <!-- Condition -->
          <div>
            <label class="form-label">
              {t["catalog.create.37"]}
            </label>
            <select name="condition" required class="form-select">
              <option value="new" selected>{t["catalog.create.38"]}</option>
              <option value="used">{t["catalog.create.39"]}</option>
              <option value="refurbished">{t["catalog.create.40"]}</option>
            </select>
          </div>

          <!-- Stock Status -->
          <div>
            <label class="form-label">
              {t["catalog.create.41"]}
            </label>
            <select name="stock_status" required class="form-select">
              <option value="instock" selected>{t["catalog.create.42"]}</option>
              <option value="outofstock">{t["catalog.create.43"]}</option>
            </select>
          </div>

          <!-- Stock Quantity Management -->
          <div class="flex items-center h-12 pt-5">
            <label class="flex items-center gap-2 cursor-pointer">
              <input
                type="checkbox"
                id="manage-stock-checkbox"
                name="manage_stock"
                class="rounded border-zinc-300 text-primary focus:ring-primary dark:border-zinc-700"
              />
              <span class="text-xs font-bold text-zinc-600 dark:text-zinc-400">
                {t["catalog.create.44"]}
              </span>
            </label>
          </div>

          <!-- Quantity Input (hidden until checkbox checked) -->
          <div id="stock-qty-container" class="hidden md:col-span-2">
            <label class="form-label">
              {t["catalog.create.45"]}
            </label>
            <input
              type="number"
              name="stock_quantity"
              min="1"
              placeholder="10"
              class="form-input"
            />
          </div>
        </div>
      </div>

      <!-- Group 3: أوزان وأبعاد الشحن (Shipping Details) -->
      <div
        id="shipping-details-group"
        class="rounded-3xl border border-zinc-200/50 bg-white/60 p-6 sm:p-8 shadow-lg backdrop-blur-md dark:border-zinc-800/50 dark:bg-zinc-900/60 space-y-6"
      >
        <h2
          class="text-lg font-bold text-zinc-900 dark:text-zinc-50 border-b border-zinc-100 pb-3 dark:border-zinc-800"
        >
          {t["catalog.create.46"]}
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
          <!-- Weight -->
          <div class="md:col-span-1">
            <label class="form-label">
              {t["catalog.create.47"]}
            </label>
            <input
              type="number"
              step="0.01"
              name="weight"
              min="0"
              placeholder="0.25"
              class="form-input"
            />
          </div>

          <!-- Length -->
          <div>
            <label class="form-label">
              {t["catalog.create.48"]}
            </label>
            <input
              type="number"
              step="0.1"
              name="length"
              min="0"
              placeholder="15"
              class="form-input"
            />
          </div>

          <!-- Width -->
          <div>
            <label class="form-label">
              {t["catalog.create.49"]}
            </label>
            <input
              type="number"
              step="0.1"
              name="width"
              min="0"
              placeholder="7"
              class="form-input"
            />
          </div>

          <!-- Height -->
          <div>
            <label class="form-label">
              {t["catalog.create.50"]}
            </label>
            <input
              type="number"
              step="0.1"
              name="height"
              min="0"
              placeholder="1"
              class="form-input"
            />
          </div>
        </div>
      </div>

      <!-- Group 3.5: Attributes and Variations (Only visible for variable products) -->
      <div
        id="variations-group"
        class="hidden rounded-3xl border border-zinc-200/50 bg-white/60 p-6 sm:p-8 shadow-lg backdrop-blur-md dark:border-zinc-800/50 dark:bg-zinc-900/60 space-y-6"
      >
        <h2
          class="text-lg font-bold text-zinc-900 dark:text-zinc-50 border-b border-zinc-100 pb-3 dark:border-zinc-800 flex items-center gap-2"
        >
          <Layers class="h-5 w-5 text-primary" />
          <span>{t["catalog.create.51"]}</span>
        </h2>

        <div class="space-y-4">
          <p class="text-xs text-zinc-500 dark:text-zinc-400">
            {t["catalog.create.52"]}
          </p>

          <div id="attributes-list" class="space-y-4">
            <!-- Dynamic attributes will be inserted here -->
          </div>

          <button
            type="button"
            id="add-attribute-btn"
            class="inline-flex items-center gap-1.5 rounded-xl border border-primary text-primary px-4 py-2 text-xs font-bold hover:bg-primary/5 transition-all"
          >
            + {t["catalog.create.53"]}
          </button>
        </div>

        <div
          id="generate-variations-container"
          class="hidden pt-4 border-t border-zinc-150 dark:border-zinc-700"
        >
          <button
            type="button"
            id="generate-variations-btn"
            class="w-full rounded-full bg-zinc-900 py-3 text-xs font-extrabold text-white hover:bg-zinc-800 transition-all dark:bg-zinc-50 dark:text-zinc-950 dark:hover:bg-zinc-200"
          >
            {t["catalog.create.54"]}
          </button>
        </div>

        <div
          id="variations-list-container"
          class="hidden space-y-4 pt-6 border-t border-zinc-150 dark:border-zinc-700"
        >
          <h3 class="text-sm font-bold text-zinc-800 dark:text-zinc-200">
            {t["catalog.create.55"]}
          </h3>
          <div class="overflow-x-auto">
            <table class="w-full text-start text-xs border-collapse">
              <thead>
                <tr
                  class="border-b border-zinc-200 dark:border-zinc-800 text-zinc-550 dark:text-zinc-405 font-bold"
                >
                  <th class="py-2.5 px-3 text-start"
                    >{t["catalog.create.56"]}</th
                  >
                  <th class="py-2.5 px-3 text-start"
                    >{t["catalog.create.57"]}</th
                  >
                  <th class="py-2.5 px-3 text-start"
                    >{t["catalog.create.58"]}</th
                  >
                  <th class="py-2.5 px-3 text-start"
                    >{t["catalog.create.59"]}</th
                  >
                  <th class="py-2.5 px-3 text-start"
                    >{t["catalog.create.60"]}</th
                  >
                </tr>
              </thead>
              <tbody
                id="variations-table-body"
                class="divide-y divide-zinc-100 dark:divide-zinc-850"
              >
                <!-- Variations generated rows -->
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Group 4: صور المنتج (Product Gallery) -->
      <div
        class="rounded-3xl border border-zinc-200/50 bg-white/60 p-6 sm:p-8 shadow-lg backdrop-blur-md dark:border-zinc-800/50 dark:bg-zinc-900/60 space-y-6"
      >
        <h2
          class="text-lg font-bold text-zinc-900 dark:text-zinc-50 border-b border-zinc-100 pb-3 dark:border-zinc-800"
        >
          {t["catalog.create.61"]}
        </h2>

        <!-- Drop Zone -->
        <div
          id="image-drop-zone"
          class="relative w-full rounded-3xl border-2 border-dashed border-zinc-300 bg-white/40 dark:border-zinc-700 dark:bg-zinc-900/40 transition-all duration-200 cursor-pointer hover:border-primary hover:bg-primary/5 dark:hover:border-primary dark:hover:bg-primary/5"
          style="min-height: 180px;"
        >
          <!-- Hidden real file input -->
          <input
            type="file"
            id="product-image-input"
            accept="image/*"
            multiple
            class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10"
          />

          <!-- Default placeholder -->
          <div
            id="drop-placeholder"
            class="flex flex-col items-center justify-center gap-3 py-10 px-6 text-center pointer-events-none"
          >
            <div
              class="w-14 h-14 rounded-2x1 bg-primary/10 dark:bg-primary/20 flex items-center justify-center"
            >
              <ImageIcon class="w-7 h-7 text-primary" />
            </div>
            <div>
              <p class="text-sm font-bold text-zinc-700 dark:text-zinc-200">
                {t["catalog.create.62"]}
              </p>
              <p class="text-xs text-zinc-400 dark:text-zinc-500 mt-1">
                {t["catalog.create.63"]}
              </p>
            </div>
          </div>

          <!-- Previews grid -->
          <div
            id="image-previews"
            class="hidden p-4 grid grid-cols-3 sm:grid-cols-4 gap-3"
          >
          </div>
        </div>

        <button
          type="button"
          id="add-more-photos-btn"
          class="hidden w-full rounded-full border border-dashed border-zinc-300 dark:border-zinc-700 py-2.5 text-xs font-bold text-zinc-500 dark:text-zinc-400 hover:border-primary hover:text-primary transition-all"
        >
          {t["catalog.create.64"]}
        </button>
      </div>

      <!-- Submit Button -->
      <button
        type="submit"
        id="submit-btn"
        class="w-full rounded-full bg-gradient-to-r from-primary via-indigo-600 to-violet-600 py-4 text-sm font-extrabold text-white shadow-lg shadow-primary/25 transition-all duration-300 hover:brightness-110 hover:shadow-xl hover:scale-[1.005] active:scale-[0.995] disabled:opacity-50 flex items-center justify-center gap-2 cursor-pointer"
      >
        <Save class="h-5 w-5" />
        <span>{t["catalog.create.65"]}</span>
      </button>
    </form>
  </Container>
</Layout>

<script>
  const form = document.getElementById(
    "create-product-form",
  ) as HTMLFormElement;
  const statusDiv = document.getElementById("form-status") as HTMLDivElement;
  const submitBtn = document.getElementById("submit-btn") as HTMLButtonElement;

  // Manage stock quantity toggle
  const manageStockCheckbox = document.getElementById(
    "manage-stock-checkbox",
  ) as HTMLInputElement;
  const stockQtyContainer = document.getElementById(
    "stock-qty-container",
  ) as HTMLDivElement;
  const stockQtyInput = form.querySelector(
    '[name="stock_quantity"]',
  ) as HTMLInputElement;

  manageStockCheckbox?.addEventListener("change", () => {
    if (manageStockCheckbox.checked) {
      stockQtyContainer.classList.remove("hidden");
      stockQtyInput.required = true;
    } else {
      stockQtyContainer.classList.add("hidden");
      stockQtyInput.required = false;
      stockQtyInput.value = "";
    }
  });

  // Brand and Supplier logo upload preview handlers
  const brandImageInput = document.getElementById(
    "brand-image-input",
  ) as HTMLInputElement;
  const brandImagePreview = document.getElementById(
    "brand-image-preview",
  ) as HTMLImageElement;
  const brandImagePlaceholder = document.getElementById(
    "brand-image-placeholder",
  ) as HTMLElement;

  brandImageInput?.addEventListener("change", () => {
    const file = brandImageInput.files?.[0];
    if (file) {
      brandImagePreview.src = URL.createObjectURL(file);
      brandImagePreview.classList.remove("hidden");
      brandImagePlaceholder.classList.add("hidden");
    }
  });

  const supplierImageInput = document.getElementById(
    "supplier-image-input",
  ) as HTMLInputElement;
  const supplierImagePreview = document.getElementById(
    "supplier-image-preview",
  ) as HTMLImageElement;
  const supplierImagePlaceholder = document.getElementById(
    "supplier-image-placeholder",
  ) as HTMLElement;

  supplierImageInput?.addEventListener("change", () => {
    const file = supplierImageInput.files?.[0];
    if (file) {
      supplierImagePreview.src = URL.createObjectURL(file);
      supplierImagePreview.classList.remove("hidden");
      supplierImagePlaceholder.classList.add("hidden");
    }
  });

  // Product Type selection toggle logic
  const typeSelect = document.getElementById(
    "product-type-select",
  ) as HTMLSelectElement;
  const variationsGroup = document.getElementById(
    "variations-group",
  ) as HTMLElement;
  const shippingGroup = document.getElementById(
    "shipping-details-group",
  ) as HTMLElement;
  const regularPriceContainer = document.getElementById(
    "regular-price-container",
  ) as HTMLElement;
  const salePriceContainer = document.getElementById(
    "sale-price-container",
  ) as HTMLElement;
  const regularPriceInput = form.querySelector(
    '[name="regular_price"]',
  ) as HTMLInputElement;

  typeSelect?.addEventListener("change", () => {
    const val = typeSelect.value;
    if (val === "variable") {
      variationsGroup.classList.remove("hidden");
      regularPriceContainer.classList.add("hidden");
      salePriceContainer.classList.add("hidden");
      regularPriceInput.required = false;
      shippingGroup.classList.remove("hidden");
    } else if (val === "virtual") {
      variationsGroup.classList.add("hidden");
      regularPriceContainer.classList.remove("hidden");
      salePriceContainer.classList.remove("hidden");
      regularPriceInput.required = true;
      shippingGroup.classList.add("hidden");
    } else {
      variationsGroup.classList.add("hidden");
      regularPriceContainer.classList.remove("hidden");
      salePriceContainer.classList.remove("hidden");
      regularPriceInput.required = true;
      shippingGroup.classList.remove("hidden");
    }
  });

  // Dynamic Attributes & Variations builder logic
  const attributesList = document.getElementById(
    "attributes-list",
  ) as HTMLElement;
  const addAttributeBtn = document.getElementById(
    "add-attribute-btn",
  ) as HTMLButtonElement;
  const generateVariationsContainer = document.getElementById(
    "generate-variations-container",
  ) as HTMLElement;
  const generateVariationsBtn = document.getElementById(
    "generate-variations-btn",
  ) as HTMLButtonElement;
  const variationsListContainer = document.getElementById(
    "variations-list-container",
  ) as HTMLElement;
  const variationsTableBody = document.getElementById(
    "variations-table-body",
  ) as HTMLElement;

  let attributesCount = 0;

  addAttributeBtn?.addEventListener("click", () => {
    attributesCount++;
    const row = document.createElement("div");
    row.className =
      "attribute-row grid grid-cols-12 gap-3 items-end bg-zinc-50/50 dark:bg-zinc-950/50 p-4 rounded-full border border-zinc-200/50 dark:border-zinc-800/50 relative";
    row.innerHTML = `
      <div class="col-span-4">
        <label class="block text-[10px] font-bold text-zinc-500 mb-1">${t["catalog.create.66"]}</label>
        <input type="text" class="attr-name w-full rounded-xl border border-zinc-200 bg-white py-2 px-3 text-xs dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-50" placeholder="${t["catalog.create.67"]}" required />
      </div>
      <div class="col-span-7">
        <label class="block text-[10px] font-bold text-zinc-500 mb-1">${t["catalog.create.68"]}</label>
        <input type="text" class="attr-options w-full rounded-xl border border-zinc-200 bg-white py-2 px-3 text-xs dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-50" placeholder="${t["catalog.create.69"]}" required />
      </div>
      <div class="col-span-1 text-center">
        <button type="button" class="remove-attr-btn text-red-500 hover:text-red-700 font-bold text-lg">&times;</button>
      </div>
    `;
    attributesList.appendChild(row);
    generateVariationsContainer.classList.remove("hidden");

    row.querySelector(".remove-attr-btn")?.addEventListener("click", () => {
      row.remove();
      if (attributesList.children.length === 0) {
        generateVariationsContainer.classList.add("hidden");
        variationsListContainer.classList.add("hidden");
      }
    });
  });

  function cartesianProduct(arrays: any[][]): any[][] {
    return arrays.reduce(
      (a, b) => {
        return a.flatMap((d) => b.map((e) => [d, e].flat()));
      },
      [[]],
    );
  }

  generateVariationsBtn?.addEventListener("click", () => {
    const attrRows = attributesList.querySelectorAll(".attribute-row");
    const attributes: { name: string; options: string[] }[] = [];

    attrRows.forEach((row) => {
      const nameInput = row.querySelector(".attr-name") as HTMLInputElement;
      const optionsInput = row.querySelector(
        ".attr-options",
      ) as HTMLInputElement;
      if (nameInput.value.trim() && optionsInput.value.trim()) {
        const options = optionsInput.value
          .split(",")
          .map((o) => o.trim())
          .filter(Boolean);
        attributes.push({ name: nameInput.value.trim(), options });
      }
    });

    if (attributes.length === 0) return;

    // Generate Cartesian combinations
    const optionArrays = attributes.map((a) => a.options);
    const combinations = cartesianProduct(optionArrays);

    variationsTableBody.innerHTML = "";

    combinations.forEach((combo, comboIdx) => {
      const comboLabel = combo
        .map((val, idx) => `${attributes[idx].name}: ${val}`)
        .join(", ");
      // Store combo values for payload mapping
      const attrMapping = combo.map((val, idx) => ({
        name: attributes[idx].name,
        option: val,
      }));

      const tr = document.createElement("tr");
      tr.className =
        "border-b border-zinc-100 dark:border-zinc-700 hover:bg-zinc-50/40 dark:hover:bg-zinc-950/20";
      tr.dataset.attrs = JSON.stringify(attrMapping);
      tr.innerHTML = `
        <td class="py-3 px-3 font-semibold text-zinc-700 dark:text-zinc-300">${comboLabel}</td>
        <td class="py-3 px-3">
          <input type="number" step="0.01" required min="0" class="var-price w-24 rounded-lg border border-zinc-200 bg-white py-1.5 px-2 text-xs dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-50" placeholder="100" />
        </td>
        <td class="py-3 px-3">
          <input type="number" step="0.01" min="0" class="var-sale-price w-24 rounded-lg border border-zinc-200 bg-white py-1.5 px-2 text-xs dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-50" placeholder="80" />
        </td>
        <td class="py-3 px-3">
          <input type="text" class="var-sku w-28 rounded-lg border border-zinc-200 bg-white py-1.5 px-2 text-xs dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-50" placeholder="SKU-suffix" />
        </td>
        <td class="py-3 px-3">
          <select class="var-stock rounded-lg border border-zinc-200 bg-white py-1.5 px-2 text-xs dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-50">
            <option value="instock" selected>${t["catalog.create.70"]}</option>
            <option value="outofstock">${t["catalog.create.71"]}</option>
          </select>
        </td>
      `;
      variationsTableBody.appendChild(tr);
    });

    variationsListContainer.classList.remove("hidden");
  });

  // Multiple Image Dropzone Management
  let selectedFiles: File[] = [];
  const dropZone = document.getElementById("image-drop-zone") as HTMLElement;
  const imageInput = document.getElementById(
    "product-image-input",
  ) as HTMLInputElement;
  const placeholder = document.getElementById(
    "drop-placeholder",
  ) as HTMLElement;
  const previewGrid = document.getElementById("image-previews") as HTMLElement;
  const addMoreBtn = document.getElementById(
    "add-more-photos-btn",
  ) as HTMLButtonElement;

  function renderPreviews() {
    if (selectedFiles.length === 0) {
      placeholder.classList.remove("hidden");
      previewGrid.classList.add("hidden");
      addMoreBtn.classList.add("hidden");
      return;
    }
    placeholder.classList.add("hidden");
    previewGrid.classList.remove("hidden");
    addMoreBtn.classList.remove("hidden");
    previewGrid.innerHTML = "";

    selectedFiles.forEach((file, idx) => {
      const url = URL.createObjectURL(file);
      const item = document.createElement("div");
      item.className =
        "relative group rounded-2x1 overflow-hidden aspect-square bg-zinc-100 dark:bg-zinc-800 border border-zinc-200/50";
      item.innerHTML = `
        <img src="${url}" class="w-full h-full object-cover" />
        ${idx === 0 ? `<span class="absolute top-1 start-1 text-[9px] font-bold bg-primary text-white px-1.5 py-0.5 rounded">${t["catalog.create.72"]}</span>` : ""}
        <button type="button" data-idx="${idx}" class="remove-img-btn absolute top-1 end-1 w-5 h-5 rounded-full bg-red-500/90 text-white flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity text-xs font-bold">&times;</button>
      `;
      previewGrid.appendChild(item);
    });

    previewGrid.querySelectorAll(".remove-img-btn").forEach((btn) => {
      btn.addEventListener("click", (e) => {
        const idx = Number((e.currentTarget as HTMLElement).dataset.idx);
        selectedFiles.splice(idx, 1);
        renderPreviews();
      });
    });
  }

  function addFiles(files: FileList | File[]) {
    Array.from(files).forEach((f) => {
      if (f.type.startsWith("image/")) selectedFiles.push(f);
    });
    renderPreviews();
  }

  imageInput?.addEventListener("change", () => {
    if (imageInput.files) addFiles(imageInput.files);
  });
  dropZone?.addEventListener("dragover", (e) => {
    e.preventDefault();
    dropZone.classList.add("border-primary");
  });
  dropZone?.addEventListener("dragleave", () => {
    dropZone.classList.remove("border-primary");
  });
  dropZone?.addEventListener("drop", (e: DragEvent) => {
    e.preventDefault();
    dropZone.classList.remove("border-primary");
    if (e.dataTransfer?.files) addFiles(e.dataTransfer.files);
  });

  addMoreBtn?.addEventListener("click", () => imageInput?.click());

  // Helper to read file as base64 object
  const readFileAsBase64 = (file: File) =>
    new Promise<{ name: string; type: string; base64: string }>((resolve) => {
      const reader = new FileReader();
      reader.onload = () => {
        resolve({
          name: file.name,
          type: file.type,
          base64: (reader.result as string).split(",")[1],
        });
      };
      reader.readAsDataURL(file);
    });

  // Form submit handler
  form?.addEventListener("submit", async (e) => {
    e.preventDefault();
    submitBtn.disabled = true;
    submitBtn.textContent = t["catalog.create.73"];

    // Verify brand logo image is selected
    const brandFile = brandImageInput.files?.[0];
    const supplierFile = supplierImageInput.files?.[0];

    if (selectedFiles.length === 0) {
      statusDiv.className =
        "p-4 mb-6 rounded-full text-sm font-semibold bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300";
      statusDiv.textContent = t["catalog.create.76"];
      statusDiv.classList.remove("hidden");
      submitBtn.disabled = false;
      submitBtn.textContent = t["catalog.create.77"];
      return;
    }

    const formData = new FormData(form);
    const productType = typeSelect.value;

    const data: Record<string, any> = {
      type: productType,
      name: formData.get("name"),
      sku: formData.get("sku"),
      categories: [Number(formData.get("category"))],
      regular_price:
        productType === "variable" ? undefined : formData.get("regular_price"),
      sale_price:
        productType === "variable"
          ? undefined
          : formData.get("sale_price") || undefined,
      short_description: formData.get("short_description"),
      description: formData.get("description") || "",
      gtin: formData.get("gtin"),
      brand: formData.get("brand"),
      supplier: formData.get("supplier"),
      mpn: formData.get("mpn") || undefined,
      condition: formData.get("condition"),
      stock_status: formData.get("stock_status"),
      manage_stock: manageStockCheckbox.checked,
      stock_quantity: manageStockCheckbox.checked
        ? Number(formData.get("stock_quantity"))
        : undefined,
      weight:
        productType === "virtual"
          ? undefined
          : formData.get("weight") || undefined,
      dimensions:
        productType !== "virtual" &&
        (formData.get("length") ||
          formData.get("width") ||
          formData.get("height"))
          ? {
              length: formData.get("length") || "",
              width: formData.get("width") || "",
              height: formData.get("height") || "",
            }
          : undefined,
    };

    try {
      statusDiv.className =
        "p-4 mb-6 rounded-full text-sm font-semibold bg-zinc-150 text-zinc-800 dark:bg-zinc-900/40 dark:text-zinc-350";
      statusDiv.textContent = t["catalog.create.78"];
      statusDiv.classList.remove("hidden");

      // Upload Brand Logo
      if (brandFile) {
        const brandBase64 = await readFileAsBase64(brandFile);
        const brandUploadRes = await fetch("/api/create-ad", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            upload_only: true,
            imageFile: brandBase64,
          }),
        });
        const brandUploadResult = await brandUploadRes.json();
        if (brandUploadResult.success && brandUploadResult.url) {
          data.brand_image = brandUploadResult.url;
        }
      }

      // Upload Supplier Logo
      if (supplierFile) {
        const supplierBase64 = await readFileAsBase64(supplierFile);
        const supplierUploadRes = await fetch("/api/create-ad", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            upload_only: true,
            imageFile: supplierBase64,
          }),
        });
        const supplierUploadResult = await supplierUploadRes.json();
        if (supplierUploadResult.success && supplierUploadResult.url) {
          data.supplier_image = supplierUploadResult.url;
        }
      }

      // Upload product main images
      const uploadedProductPhotos = await Promise.all(
        selectedFiles.map(readFileAsBase64),
      );
      const imageUrls: string[] = [];
      for (const file of uploadedProductPhotos) {
        const uploadRes = await fetch("/api/create-ad", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            upload_only: true,
            imageFile: file,
          }),
        });
        const uploadResult = await uploadRes.json();
        if (uploadResult.success && uploadResult.url) {
          imageUrls.push(uploadResult.url);
        }
      }

      if (imageUrls.length === 0) {
        throw new Error(t["catalog.create.81"]);
      }
      data.images = imageUrls;

      // If variable product, extract attributes list and variations rows
      if (productType === "variable") {
        const attributes: { name: string; options: string[] }[] = [];
        const attrRows = attributesList.querySelectorAll(".attribute-row");
        attrRows.forEach((row) => {
          const nameInput = row.querySelector(".attr-name") as HTMLInputElement;
          const optionsInput = row.querySelector(
            ".attr-options",
          ) as HTMLInputElement;
          if (nameInput.value.trim() && optionsInput.value.trim()) {
            const options = optionsInput.value
              .split(",")
              .map((o) => o.trim())
              .filter(Boolean);
            attributes.push({ name: nameInput.value.trim(), options });
          }
        });
        data.attributes = attributes;

        // Extract variations rows from table
        const variations: any[] = [];
        const varRows = variationsTableBody.querySelectorAll("tr");
        varRows.forEach((row) => {
          const priceInput = row.querySelector(
            ".var-price",
          ) as HTMLInputElement;
          const saleInput = row.querySelector(
            ".var-sale-price",
          ) as HTMLInputElement;
          const skuInput = row.querySelector(".var-sku") as HTMLInputElement;
          const stockSelect = row.querySelector(
            ".var-stock",
          ) as HTMLSelectElement;
          const attrs = JSON.parse(row.dataset.attrs || "[]");

          variations.push({
            regular_price: priceInput.value,
            sale_price: saleInput.value || undefined,
            sku: skuInput.value ? `${data.sku}-${skuInput.value}` : undefined,
            stock_status: stockSelect.value,
            attributes: attrs,
          });
        });
        data.variations = variations;
      }

      statusDiv.textContent = t["catalog.create.82"];

      // Send to products create API endpoint
      const response = await fetch("/api/products/create", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(data),
      });

      const result = await response.json();

      if (result.success) {
        statusDiv.className =
          "p-4 mb-6 rounded-full text-sm font-semibold bg-emerald-100 text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300";
        statusDiv.textContent = t["catalog.create.83"];
        form.reset();
        selectedFiles = [];
        renderPreviews();

        setTimeout(() => {
          window.location.href = `/${t["catalog.create.84"]}/seller/products`;
        }, 1500);
      } else {
        throw new Error(result.error || "Failed to save product");
      }
    } catch (err: any) {
      statusDiv.className =
        "p-4 mb-6 rounded-full text-sm font-semibold bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300";
      statusDiv.textContent = `${t["catalog.create.85"]} ${err.message}`;
      submitBtn.disabled = false;
      submitBtn.textContent = t["catalog.create.86"];
    }
  });
  // Listen for AI prefill events
  window.addEventListener("meamart:prefill", (e: any) => {
    const prefillData = e.detail;
    if (!prefillData || !form) return;

    if (prefillData.title) {
      const nameInput = form.querySelector('[name="name"]') as HTMLInputElement;
      if (nameInput) {
        nameInput.value = prefillData.title;
        nameInput.dispatchEvent(new Event("input"));
      }
    }
    if (prefillData.description) {
      const descInput = form.querySelector(
        '[name="description"]',
      ) as HTMLTextAreaElement;
      if (descInput) {
        descInput.value = prefillData.description;
        descInput.dispatchEvent(new Event("input"));
      }
    }
    if (prefillData.price) {
      const priceInput = form.querySelector(
        '[name="regular_price"]',
      ) as HTMLInputElement;
      if (priceInput) {
        priceInput.value = String(prefillData.price);
        priceInput.dispatchEvent(new Event("input"));
      }
    }
    if (prefillData.sku) {
      const skuInput = form.querySelector('[name="sku"]') as HTMLInputElement;
      if (skuInput) {
        skuInput.value = String(prefillData.sku);
        skuInput.dispatchEvent(new Event("input"));
      }
    }
    if (prefillData.brand) {
      const brandInput = form.querySelector(
        '[name="brand"]',
      ) as HTMLInputElement;
      if (brandInput) {
        brandInput.value = String(prefillData.brand);
        brandInput.dispatchEvent(new Event("input"));
      }
    }
    if (prefillData.condition) {
      const conditionSelect = form.querySelector(
        '[name="condition"]',
      ) as HTMLSelectElement;
      if (conditionSelect) {
        // meamart expects new/used etc. Try to match.
        const val = prefillData.condition.toLowerCase();
        if (val.includes("new")) conditionSelect.value = "new";
        else if (val.includes("used")) conditionSelect.value = "used";
        conditionSelect.dispatchEvent(new Event("change"));
      }
    }

    // Handle custom fields (specs) as attributes in the product
    const attributesList = document.getElementById("attributes-list");
    const addAttributeBtn = document.getElementById("add-attribute-btn");

    if (attributesList && addAttributeBtn) {
      Object.keys(prefillData).forEach((key) => {
        if (key.startsWith("custom_field_") || key.startsWith("custom_")) {
          const val = prefillData[key];
          if (!val) return;

          let attrName = key
            .replace("custom_field_", "")
            .replace("custom_", "")
            .replace(/_/g, " ");
          // capitalize
          attrName = attrName.charAt(0).toUpperCase() + attrName.slice(1);

          addAttributeBtn.click();

          const rows = attributesList.querySelectorAll(".attribute-row");
          if (rows.length > 0) {
            const lastRow = rows[rows.length - 1];
            const nameInput = lastRow.querySelector(
              ".attr-name",
            ) as HTMLInputElement;
            const optionsInput = lastRow.querySelector(
              ".attr-options",
            ) as HTMLInputElement;

            if (nameInput) nameInput.value = attrName;
            if (optionsInput) optionsInput.value = String(val);
          }
        }
      });
    }

    form.scrollIntoView({ behavior: "smooth", block: "start" });
  });
</script>
