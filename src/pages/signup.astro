---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="تسجيل عملك - مي مارت">
  <div class="auth-container">
    <div class="auth-card">
      <div class="auth-header">
        <h1>سجّل عملك التجاري</h1>
        <p>انضم إلى آلاف الأعمال التجارية الموثوقة</p>
      </div>

      <form id="signupForm" class="auth-form">
        <!-- Business Name -->
        <div class="form-group">
          <label for="businessName">اسم العمل التجاري *</label>
          <input
            type="text"
            id="businessName"
            name="businessName"
            placeholder="مثال: الكهرباء والإضاءة"
            required
          />
          <span class="error-message" id="businessNameError"></span>
        </div>

        <!-- Email -->
        <div class="form-group">
          <label for="email">البريد الإلكتروني *</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="your@email.com"
            required
          />
          <span class="error-message" id="emailError"></span>
        </div>

        <!-- Country -->
        <div class="form-group">
          <label for="country">الدولة *</label>
          <input
            type="text"
            id="country"
            name="country"
            placeholder="مصر"
            required
          />
          <span class="error-message" id="countryError"></span>
        </div>

        <!-- City -->
        <div class="form-group">
          <label for="city">المدينة *</label>
          <input
            type="text"
            id="city"
            name="city"
            placeholder="القاهرة"
            required
          />
          <span class="error-message" id="cityError"></span>
        </div>

        <!-- WhatsApp -->
        <div class="form-group">
          <label for="whatsapp">رقم واتس (بدون الرمز الدولي) *</label>
          <input
            type="tel"
            id="whatsapp"
            name="whatsapp"
            placeholder="0501234567"
            pattern="[0-9]{6,12}"
            required
          />
          <span class="error-message" id="whatsappError"></span>
        </div>

        <!-- Category -->
        <div class="form-group">
          <label for="category">فئة العمل *</label>
          <select id="category" name="category" required>
            <option value="">اختر الفئة</option>
            <option value="electronics">💻 إلكترونيات</option>
            <option value="food">🍔 طعام و مشروبات</option>
            <option value="fashion">👗 الموضة</option>
            <option value="real-estate">🏠 العقارات</option>
            <option value="services">🛠️ الخدمات</option>
            <option value="health">💊 الصحة و الجمال</option>
            <option value="automotive">🚗 السيارات</option>
            <option value="education">📚 التعليم</option>
          </select>
          <span class="error-message" id="categoryError"></span>
        </div>

        <!-- Description -->
        <div class="form-group">
          <label for="description">وصف العمل</label>
          <textarea
            id="description"
            name="description"
            placeholder="وصف قصير عن عملك التجاري..."
            rows="4"
          ></textarea>
          <span class="error-message" id="descriptionError"></span>
        </div>

        <!-- Password -->
        <div class="form-group">
          <label for="password">كلمة المرور *</label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="كلمة قوية"
            minlength="8"
            required
          />
          <span class="error-message" id="passwordError"></span>
        </div>

        <!-- Confirm Password -->
        <div class="form-group">
          <label for="confirmPassword">تأكيد كلمة المرور *</label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            placeholder="أعد إدخال كلمة المرور"
            minlength="8"
            required
          />
          <span class="error-message" id="confirmPasswordError"></span>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="submit-btn">إنشاء حساب</button>

        <!-- Message -->
        <div id="messageContainer"></div>
      </form>

      <!-- Login Link -->
      <p class="auth-footer">
        لديك حساب بالفعل؟ <a href="/login">تسجيل الدخول</a>
      </p>
    </div>
  </div>

  <script type="module" is:inline>
    const form = document.getElementById('signupForm');
    const messageContainer = document.getElementById('messageContainer');

    if (form && messageContainer) {
      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        // Validate
        const passwordEl = document.getElementById('password');
        const confirmPasswordEl = document.getElementById('confirmPassword');
        const whatsappEl = document.getElementById('whatsapp');

        const password = passwordEl ? passwordEl.value : '';
        const confirmPassword = confirmPasswordEl ? confirmPasswordEl.value : '';

        if (password !== confirmPassword) {
          showError('كلمات المرور غير متطابقة');
          return;
        }

        if (password.length < 8) {
          showError('كلمة المرور يجب أن تكون 8 أحرف على الأقل');
          return;
        }

        const whatsapp = whatsappEl ? whatsappEl.value : '';
        if (!/^\d{6,12}$/.test(whatsapp)) {
          showError('رقم الواتس يجب أن يكون 6-12 رقم');
          return;
        }

        // Prepare data
        const formData = new FormData(form);
        const data = {
          businessName: formData.get('businessName'),
          email: formData.get('email'),
          country: formData.get('country'),
          city: formData.get('city'),
          whatsapp: formData.get('whatsapp'),
          category: formData.get('category'),
          description: formData.get('description'),
          password: formData.get('password'),
        };

        try {
          showMessage('جاري التسجيل...', 'loading');

          // API call
          const apiUrl = import.meta.env.PUBLIC_DASHBOARD_API_URL || 'https://cd155f50.meamart-dashboard.pages.dev';
          const response = await fetch(`${apiUrl}/api/auth/signup`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify(data),
          });

          if (response.ok) {
            const result = await response.json();
            localStorage.setItem('token', result.token);
            showMessage('✅ تم التسجيل بنجاح! جاري التوجيه...', 'success');
            setTimeout(() => {
              window.location.href = '/dashboard';
            }, 2000);
          } else {
            const error = await response.json();
            showError(error.message || 'حدث خطأ في التسجيل');
          }
        } catch (error) {
          const err = error;
          showError('خطأ في الاتصال: ' + (err instanceof Error ? err.message : 'خطأ غير معروف'));
        }
      });

      function showMessage(msg, type = 'info') {
        if (messageContainer) {
          messageContainer.innerHTML = `<div class="message ${type}">${msg}</div>`;
        }
      }

      function showError(msg) {
        showMessage(msg, 'error');
      }
    }
  </script>
</BaseLayout>

<style>
  .auth-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: 20px;
    direction: rtl;
  }

  .auth-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    padding: 40px;
    max-width: 500px;
    width: 100%;
  }

  .auth-header {
    text-align: center;
    margin-bottom: 30px;
  }

  .auth-header h1 {
    font-size: 28px;
    margin: 0 0 10px 0;
    color: #333;
  }

  .auth-header p {
    color: #666;
    margin: 0;
    font-size: 14px;
  }

  .auth-form {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-group label {
    font-weight: 600;
    margin-bottom: 6px;
    color: #333;
    font-size: 14px;
  }

  .form-group input,
  .form-group textarea,
  .form-group select {
    padding: 12px;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-family: inherit;
    font-size: 14px;
    color: #333;
    transition: border-color 0.3s;
  }

  .form-group input:focus,
  .form-group textarea:focus,
  .form-group select:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 100px;
  }

  .error-message {
    color: #e74c3c;
    font-size: 12px;
    margin-top: 4px;
  }

  .submit-btn {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 12px;
    border: none;
    border-radius: 6px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    margin-top: 10px;
  }

  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
  }

  .submit-btn:active {
    transform: translateY(0);
  }

  .message {
    padding: 12px;
    border-radius: 6px;
    text-align: center;
    font-size: 14px;
  }

  .message.success {
    background: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
  }

  .message.error {
    background: #f8d7da;
    color: #721c24;
    border: 1px solid #f5c6cb;
  }

  .message.loading {
    background: #d1ecf1;
    color: #0c5460;
    border: 1px solid #bee5eb;
  }

  .auth-footer {
    text-align: center;
    color: #666;
    font-size: 14px;
    margin-top: 20px;
  }

  .auth-footer a {
    color: #667eea;
    text-decoration: none;
    font-weight: 600;
  }

  .auth-footer a:hover {
    text-decoration: underline;
  }

  @media (max-width: 600px) {
    .auth-card {
      padding: 20px;
    }

    .auth-header h1 {
      font-size: 24px;
    }
  }
</style>
