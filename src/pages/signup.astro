---
import BaseLayout from "@layouts/BaseLayout.astro";
import AppShell from "@components/app/AppShell.astro";
import { Icon } from "astro-icon/components";
import SignupForm from "@components/auth/SignupForm.vue";
import { useTranslations } from "@util/useTranslations";

const { t } = useTranslations(Astro.request);
---

<BaseLayout title="إنشاء متجر جديد" description="انضم إلى منصة MeaMart وابدأ بيع منتجاتك عبر WhatsApp Business">
  <AppShell>
    <div class="mx-auto max-w-4xl px-6 py-12">
      <!-- Header -->
      <div class="mb-12 text-center">
        <h1 class="text-4xl font-bold text-gray-900 dark:text-gray-50">
          انشئ متجرك الآن 🚀
        </h1>
        <p class="mt-4 text-lg text-gray-600 dark:text-gray-400">
          منصة مجانية لعرض منتجاتك وبيعها عبر WhatsApp Business مباشرة
        </p>
      </div>

      <!-- Main Content -->
      <div class="grid gap-8 lg:grid-cols-2">
        <!-- Left: Form -->
        <div class="rounded-lg border border-gray-200 bg-white p-8 shadow-sm dark:border-gray-800 dark:bg-gray-900">
          <h2 class="mb-6 text-2xl font-bold text-gray-900 dark:text-gray-50">
            إنشاء حسابك
          </h2>
          <SignupForm client:load />
        </div>

        <!-- Right: Features -->
        <div class="space-y-6">
          <div class="rounded-lg border border-blue-200 bg-blue-50 p-6 dark:border-blue-900 dark:bg-blue-950">
            <h3 class="flex items-center gap-2 text-lg font-bold text-blue-900 dark:text-blue-50">
              <Icon name="tabler:check" class="h-6 w-6 text-blue-600" />
              لماذا MeaMart؟
            </h3>
            <ul class="mt-4 space-y-3 text-sm text-blue-800 dark:text-blue-200">
              <li class="flex items-start gap-2">
                <Icon name="tabler:circle-check" class="mt-1 h-5 w-5 flex-shrink-0" />
                <span>بيع مباشر عبر WhatsApp Business - بدون وسيط</span>
              </li>
              <li class="flex items-start gap-2">
                <Icon name="tabler:circle-check" class="mt-1 h-5 w-5 flex-shrink-0" />
                <span>توافق تام مع Google Merchant Center</span>
              </li>
              <li class="flex items-start gap-2">
                <Icon name="tabler:circle-check" class="mt-1 h-5 w-5 flex-shrink-0" />
                <span>إدارة المنتجات عبر Google Sheets</span>
              </li>
              <li class="flex items-start gap-2">
                <Icon name="tabler:circle-check" class="mt-1 h-5 w-5 flex-shrink-0" />
                <span>مجاني بنسبة 100% - لا توجد رسوم مخفية</span>
              </li>
              <li class="flex items-start gap-2">
                <Icon name="tabler:circle-check" class="mt-1 h-5 w-5 flex-shrink-0" />
                <span>تخزين غير محدود للمنتجات</span>
              </li>
              <li class="flex items-start gap-2">
                <Icon name="tabler:circle-check" class="mt-1 h-5 w-5 flex-shrink-0" />
                <span>دعم التعريب الكامل (العربية والإنجليزية)</span>
              </li>
            </ul>
          </div>

          <!-- Integration Preview -->
          <div class="rounded-lg border border-purple-200 bg-purple-50 p-6 dark:border-purple-900 dark:bg-purple-950">
            <h3 class="flex items-center gap-2 text-lg font-bold text-purple-900 dark:text-purple-50">
              <Icon name="tabler:brand-whatsapp" class="h-6 w-6 text-green-600" />
              التكاملات المتاحة
            </h3>
            <div class="mt-4 space-y-2 text-sm text-purple-800 dark:text-purple-200">
              <div class="flex items-center gap-2">
                <div class="h-2 w-2 rounded-full bg-green-500"></div>
                <span>WhatsApp Business (فوري)</span>
              </div>
              <div class="flex items-center gap-2">
                <div class="h-2 w-2 rounded-full bg-blue-500"></div>
                <span>Google Sheets (تلقائي)</span>
              </div>
              <div class="flex items-center gap-2">
                <div class="h-2 w-2 rounded-full bg-orange-500"></div>
                <span>Google Merchant Center (تلقائي)</span>
              </div>
              <div class="flex items-center gap-2">
                <div class="h-2 w-2 rounded-full bg-indigo-500"></div>
                <span>Google Shopping (عبر Merchant)</span>
              </div>
            </div>
          </div>

          <!-- FAQ -->
          <div class="rounded-lg border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-800 dark:bg-gray-900">
            <h3 class="flex items-center gap-2 text-lg font-bold text-gray-900 dark:text-gray-50">
              <Icon name="tabler:help-circle" class="h-6 w-6 text-gray-600" />
              أسئلة شائعة
            </h3>
            <div class="mt-4 space-y-3 text-sm text-gray-600 dark:text-gray-400">
              <details class="cursor-pointer">
                <summary class="font-semibold text-gray-900 dark:text-gray-50">
                  هل أحتاج كود بطاقة ائتمان؟
                </summary>
                <p class="mt-2">
                  لا بالمرة! المنصة مجانية 100% - فقط اسم مستخدم بريد إلكتروني ورقم واتس
                </p>
              </details>
              <details class="cursor-pointer">
                <summary class="font-semibold text-gray-900 dark:text-gray-50">
                  كيف أضيف منتجاتي؟
                </summary>
                <p class="mt-2">
                  أنشئ جدول في Google Sheets بمنتجاتك ثم وصل الجدول في الإعدادات - تقوم المنصة بتحديث المنتجات تلقائياً
                </p>
              </details>
              <details class="cursor-pointer">
                <summary class="font-semibold text-gray-900 dark:text-gray-50">
                  كيف يتصل بي العملاء؟
                </summary>
                <p class="mt-2">
                  كل منتج يحتوي على زر WhatsApp مباشر - يفتح chat مع رقمك على الفور
                </p>
              </details>
            </div>
          </div>
        </div>
      </div>

      <!-- Bottom CTA -->
      <div class="mt-12 rounded-lg border border-green-200 bg-green-50 p-8 text-center dark:border-green-900 dark:bg-green-950">
        <p class="text-sm font-medium text-green-900 dark:text-green-50">
          ✅ استأذن من MeaMart - اختيار آمن لعملك
        </p>
        <p class="mt-2 text-xs text-green-700 dark:text-green-400">
          ستكون بيانات متجرك آمنة وخاصة وغير مشاركة مع أي أطراف ثالثة
        </p>
      </div>
    </div>
  </AppShell>
</BaseLayout>
