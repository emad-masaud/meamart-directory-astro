---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="تسجيل الدخول - مي مارت">
  <div class="auth-container">
    <div class="auth-card">
      <div class="auth-header">
        <h1>تسجيل الدخول</h1>
        <p>أدخل بيانات عملك التجاري</p>
      </div>

      <form id="loginForm" class="auth-form">
        <!-- Email -->
        <div class="form-group">
          <label for="email">البريد الإلكتروني *</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="your@email.com"
            required
          />
          <span class="error-message" id="emailError"></span>
        </div>

        <!-- Password -->
        <div class="form-group">
          <label for="password">كلمة المرور *</label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="أدخل كلمة المرور"
            required
          />
          <span class="error-message" id="passwordError"></span>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="submit-btn">دخول</button>

        <!-- Message -->
        <div id="messageContainer"></div>

        <!-- Google OAuth -->
        <div class="divider">أو</div>
        <button type="button" class="google-btn" id="googleBtn">
          <span>🔗 دخول عبر Google</span>
        </button>
      </form>

      <!-- Forgot Password & Signup Links -->
      <div class="auth-footer">
        <p><a href="/forgot-password">هل نسيت كلمة المرور؟</a></p>
        <p>ليس لديك حساب؟ <a href="/signup">إنشاء حساب جديد</a></p>
      </div>
    </div>
  </div>

  <script type="module" is:inline>
    const loginForm = document.getElementById('loginForm');
    const googleBtn = document.getElementById('googleBtn');
    const messageContainer = document.getElementById('messageContainer');
    const apiUrl = import.meta.env.PUBLIC_DASHBOARD_API_URL || 'https://cd155f50.meamart-dashboard.pages.dev';

    if (loginForm && messageContainer) {
      // Form submission
      loginForm.addEventListener('submit', async (e) => {
        e.preventDefault();

        const emailEl = document.getElementById('email');
        const passwordEl = document.getElementById('password');
        const email = emailEl ? emailEl.value : '';
        const password = passwordEl ? passwordEl.value : '';

        try {
          showMessage('جاري تسجيل الدخول...', 'loading');

          const response = await fetch(`${apiUrl}/api/auth/login`, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ email, password }),
          });

          if (response.ok) {
            const result = await response.json();
            localStorage.setItem('token', result.token);
            showMessage('✅ تم تسجيل الدخول بنجاح! جاري التوجيه...', 'success');
            setTimeout(() => {
              window.location.href = '/dashboard';
            }, 2000);
          } else {
            const error = await response.json();
            showError(error.message || 'بيانات دخول غير صحيحة');
          }
        } catch (error) {
          const err = error;
          showError('خطأ في الاتصال: ' + (err instanceof Error ? err.message : 'خطأ غير معروف'));
        }
      });

      // Google OAuth
      if (googleBtn) {
        googleBtn.addEventListener('click', async (e) => {
          e.preventDefault();

          try {
            showMessage('جاري توجيهك إلى Google...', 'loading');

            const response = await fetch(`${apiUrl}/api/auth/google/url`);
            if (response.ok) {
              const data = await response.json();
              window.location.href = data.url;
            } else {
              showError('خطأ في الاتصال بـ Google');
            }
          } catch (error) {
            const err = error;
            showError('خطأ: ' + (err instanceof Error ? err.message : 'خطأ غير معروف'));
          }
        });
      }

      function showMessage(msg, type = 'info') {
        if (messageContainer) {
          messageContainer.innerHTML = `<div class="message ${type}">${msg}</div>`;
        }
      }

      function showError(msg) {
        showMessage(msg, 'error');
      }
    }
  </script>
</BaseLayout>

<style>
  .auth-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: 20px;
    direction: rtl;
  }

  .auth-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    padding: 40px;
    max-width: 400px;
    width: 100%;
  }

  .auth-header {
    text-align: center;
    margin-bottom: 30px;
  }

  .auth-header h1 {
    font-size: 28px;
    margin: 0 0 10px 0;
    color: #333;
  }

  .auth-header p {
    color: #666;
    margin: 0;
    font-size: 14px;
  }

  .auth-form {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-group label {
    font-weight: 600;
    margin-bottom: 6px;
    color: #333;
    font-size: 14px;
  }

  .form-group input {
    padding: 12px;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-family: inherit;
    font-size: 14px;
    color: #333;
    transition: border-color 0.3s;
  }

  .form-group input:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
  }

  .error-message {
    color: #e74c3c;
    font-size: 12px;
    margin-top: 4px;
  }

  .submit-btn {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 12px;
    border: none;
    border-radius: 6px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    margin-top: 10px;
  }

  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
  }

  .submit-btn:active {
    transform: translateY(0);
  }

  .divider {
    text-align: center;
    color: #999;
    font-size: 12px;
    margin: 15px 0;
  }

  .google-btn {
    background: white;
    border: 1px solid #ddd;
    padding: 12px;
    border-radius: 6px;
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
    transition: all 0.3s;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .google-btn:hover {
    background: #f9f9f9;
    border-color: #667eea;
  }

  .message {
    padding: 12px;
    border-radius: 6px;
    text-align: center;
    font-size: 14px;
  }

  .message.success {
    background: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
  }

  .message.error {
    background: #f8d7da;
    color: #721c24;
    border: 1px solid #f5c6cb;
  }

  .message.loading {
    background: #d1ecf1;
    color: #0c5460;
    border: 1px solid #bee5eb;
  }

  .auth-footer {
    text-align: center;
    margin-top: 30px;
    padding-top: 30px;
    border-top: 1px solid #eee;
  }

  .auth-footer p {
    color: #666;
    font-size: 14px;
    margin: 8px 0;
  }

  .auth-footer a {
    color: #667eea;
    text-decoration: none;
    font-weight: 600;
  }

  .auth-footer a:hover {
    text-decoration: underline;
  }

  @media (max-width: 600px) {
    .auth-card {
      padding: 20px;
    }

    .auth-header h1 {
      font-size: 24px;
    }
  }
</style>
